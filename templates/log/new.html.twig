{% extends 'base.html.twig' %}

{% block title %}New ReadLog{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const booksSelect = document.querySelector('#read_log_book');

            const choices = new Choices(booksSelect, {
                searchEnabled: true,
                searchPlaceholderValue: 'Search',
                itemSelectText: '',
                noResultsText: 'No books found',
                noChoicesText: 'No books available',
                classNames: {
                    containerInner: ['form-control']
                }
                // shouldSort: false
            });
        });
    </script>
{% endblock %}

{% block body %}
<div class="form-container">
    <div class="form-header">
        <h1>📖 Create New Reading Log</h1>
        <a href="{{ path('app_dashboard_reading_log') }}" class="btn-base btn-back">← Back to Reading Log</a>
    </div>

    <div class="form-card">
        {{ include('log/_form.html.twig') }}
    </div>
</div>
{% endblock %}
