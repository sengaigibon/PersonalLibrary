{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/rr-styles.css') }}">
{% endblock %}

<div class="content-header">
    <h1>Dashboard</h1>
</div>

<div class="dashboard-content">
    <div class="dashboard-card">
        <h3>Books read this year ({{ thisYear }})</h3>
    </div>
    <div class="dashboard-card">
        <h3>Reading speed this year ({{ thisYear }})</h3>
    </div>
</div>
