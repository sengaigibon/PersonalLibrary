{% extends 'base.html.twig' %}

{% block title %}Book index{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/common.css') }}">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        function updateStatus(bookId, buttonElement, status) {
            const dataPath = buttonElement.getAttribute('data-path');
            let text, statusMessage;
            switch (status) {
                case 'reading':
                    text = '📖 Reading now...';
                    statusMessage = '📖 Reading';
                    break;
                case 'finished':
                    text = statusMessage = '✅ Finished';
                    break;
            }

            fetch(dataPath, {
                method: 'GET',
                headers: {
                    'Content-Type': 'application/json',
                    'X-Requested-With': 'XMLHttpRequest'
                }
            })
                .then(response => {
                    return response.json().then(data => ({
                        status: response.status,
                        data: data
                    }));
                })
                .then(function(result) {
                    if (result.status === 200 && result.data.success) {
                        buttonElement.textContent = text;
                        buttonElement.style.backgroundColor = '#95a5a6';
                        buttonElement.style.cursor = 'not-allowed';
                        buttonElement.style.pointerEvents = 'none';
                        buttonElement.onclick = null;

                        const row = document.getElementById('tr-book-' + bookId);
                        if (row) {
                            const statusCell = row.querySelector('.read-status');
                            if (statusCell) {
                                statusCell.innerHTML = `<span class="status-${status}">${statusMessage}</span>`;
                            }
                        }
                    } else if (result.status === 400 && result.data.error) {
                        alert('Error: ' + result.data.error);
                    } else {
                        alert('Unexpected response from server');
                    }
                })
                .catch(error => {
                    console.error('Error making request:', error);
                    alert('Network error occurred');
                });
        }
    </script>
{% endblock %}

{% block body %}
<div class="books-container">
    <div class="books-header">
        <h1>📚 Library</h1>
        <a href="{{ path('app_book_new') }}" class="btn-base btn-new">+ Add New Book</a>
    </div>

    <table class="table modern-table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Title</th>
                <th>Author</th>
                <th>Pages</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for book in books %}
            {% if book is not null and book is not empty %}
            <tr id="tr-book-{{ book.id }}">
                <td>{{ book.id }}</td>
                <td class="book-title">{{ book.title ?? '' }}</td>
                <td class="book-author">{{ book.author ?? '' }}</td>
                <td class="pages">{{ book.pages ?? '?' }}</td>
                <td class="read-status">
                    {% set hasFinishedReading = false %}
                    {% set isCurrentlyReading = false %}

                    {% for readLog in book.readLogs %}
                        {% if readLog.startDate is not empty and readLog.finishDate is empty %}
                            {% set isCurrentlyReading = true %}
                        {% elseif readLog.finishDate is not empty %}
                            {% set hasFinishedReading = true %}
                        {% endif %}
                    {% endfor %}

                    {% if isCurrentlyReading %}
                        <span class="status-reading">📖 Reading</span>
                    {% elseif hasFinishedReading %}
                        <span class="status-finished">✅ Finished</span>
                    {% else %}
                        <span class="status-unread">📚 Unread</span>
                    {% endif %}
                </td>
                <td>
                    {% if book.id is not null %}
                    <a href="{{ path('app_book_show', {'id': book.id}) }}" class="action-btn btn-show">👁 Show</a>
                    <a href="{{ path('app_book_edit', {'id': book.id}) }}" class="action-btn btn-edit">✏️ Edit</a>

                    {% if isCurrentlyReading %}
                        <a href="#" class="action-btn btn-finish-reading" data-path="{{ path('app_book_finish', {'id': book.id}) }}" onclick="updateStatus({{ book.id }}, this, 'finished'); return false;">✅ Finish Reading</a>
                    {% else %}
                        <a href="#" class="action-btn btn-start-reading" data-path="{{ path('app_book_start', {'id': book.id}) }}" onclick="updateStatus({{ book.id }}, this, 'reading'); return false;">📖 Start Reading</a>
                    {% endif %}

                    {% endif %}
                </td>
            </tr>
            {% endif %}
        {% else %}
            <tr>
                <td colspan="6" class="no-records">📖 No books found. Start building your library!</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {{ include('components/pagination.html.twig', {
        'route_name': 'app_dashboard_books',
        'route_params': {},
        'item_name': 'books'
    }) }}
</div>
{% endblock %}
