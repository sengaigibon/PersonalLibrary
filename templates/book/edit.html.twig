{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}
<div class="edit-container">
    <div class="edit-header">
        <h1>✏️ Edit Book</h1>
        <a href="{{ path('app_dashboard_books', pagination_params|default({})) }}" class="btn-base btn-back">← Back to Library</a>
    </div>

    <div class="form-card">
        {{ include('book/_form.html.twig', {'button_label': 'Update Book'}) }}
    </div>

    <div class="actions-card">
        <div class="info-section">
            <p style="color: #7f8c8d; margin: 0; font-style: italic;">
                💡 Use the search button next to ISBN to auto-fill book details
            </p>
        </div>

        <div class="danger-zone">
            <h3>⚠️ Danger Zone</h3>
            {{ include('book/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
